import Link from "next/link";
export function Pagination({ page, totalPages }: { page: number; totalPages: number }) { return <div className="mt-10 flex items-center justify-center gap-3"><Link aria-disabled={page <= 1} className={`rounded-xl border px-4 py-2 text-sm font-bold ${page <= 1 ? "pointer-events-none text-slate-300" : "border-slate-200 bg-white text-slate-700 hover:border-[#ec1c24]"}`} href={`/?page=${Math.max(1, page - 1)}`}>Previous</Link><span className="text-sm font-semibold text-slate-500">Page {page} of {totalPages}</span><Link aria-disabled={page >= totalPages} className={`rounded-xl border px-4 py-2 text-sm font-bold ${page >= totalPages ? "pointer-events-none text-slate-300" : "border-slate-200 bg-white text-slate-700 hover:border-[#ec1c24]"}`} href={`/?page=${Math.min(totalPages, page + 1)}`}>Next</Link></div>; }
